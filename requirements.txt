# ShopTalk — pinned for reproducibility (Python 3.12, run inside .venv-shoptalk)
# Install: pip install -r requirements.txt

# --- Core / config / utils ---
pyyaml==6.0.2
pydantic==2.9.2
python-dotenv==1.0.1

# --- Data & EDA ---
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0
matplotlib==3.9.2
seaborn==0.13.2
jupyter==1.1.1

# --- Embeddings / fine-tuning / transformers stack ---
torch==2.4.1
transformers==4.45.2
sentence-transformers==3.2.1
peft==0.13.2
accelerate==1.0.1
pillow==10.4.0

# --- Vector DB ---
chromadb==0.5.15

# --- Agent / LLM orchestration ---
# Generator LLM is Groq-hosted (not local Ollama) — avoids a 5-7GB model competing for the
# M3's 16GB unified memory alongside FastAPI/Streamlit/Chroma/Whisper. See PROJECT_REPORT.md
# §5 for the pivot rationale; doesn't change Phase 3's retrieval evals (orthogonal to the LLM).
langgraph==0.2.39
langchain-core==0.3.13
groq==1.4.0
redis==5.1.1

# --- API / UI ---
fastapi==0.115.2
uvicorn[standard]==0.31.1
streamlit==1.39.0
httpx==0.27.2

# --- Voice (speech-to-text / text-to-speech) ---
faster-whisper==1.0.3
# piper-tts==1.4.2 ships a real cp39-abi3 arm64 wheel (verified live: `pip install
# piper-tts==1.4.2` succeeds and `from piper import PiperVoice` imports cleanly). An older
# project note claimed Piper wasn't pip-installable on Python 3.12/arm64 because
# `piper-phonemize` shipped no matching wheel — true for the release checked at the time,
# fixed in this one. Voices (.onnx + .onnx.json) are downloaded separately, once, via
# `python -m piper.download_voices` — see README "Voice mode" setup.
piper-tts==1.4.2

# --- Testing / quality ---
pytest==8.3.3
pytest-cov==5.0.0
black==24.10.0
ruff==0.7.0
pre-commit==4.0.1
