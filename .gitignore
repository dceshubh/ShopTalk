# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtual environments
.venv-shoptalk/
.venv/

# Data & model artifacts (never commit — large, reproducible from pipeline).
# data/eval/ is intentionally NOT listed here — it holds hand-written eval source content
# (golden_set.json), small and not reproducible from any pipeline step.
data/chroma/
data/images/
data/kaggle_process/
data/processed/
data/raw/
weights/
*.parquet
*.tar
*.tar.gz
chroma_db/

# Secrets
.env
*.key

# IDE / OS
.vscode/
.DS_Store

# Notebook checkpoints
.ipynb_checkpoints/
.claude/
