# ShopTalk conda env (alternative to venv+pip; per FAQ).
# Create:  conda env create -f environment.yml
# Update:  conda env update -f environment.yml --prune
name: shoptalk
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements.txt
